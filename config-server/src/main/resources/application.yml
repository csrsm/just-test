server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/csrsm/just-test/
          searchPaths:  /config-server
          clone-on-start: true
          force-pull: true
          username:
          password:
      label: master